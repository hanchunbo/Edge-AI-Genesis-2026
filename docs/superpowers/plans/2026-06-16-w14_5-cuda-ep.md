# W14.5 CUDA EP 接入 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 `w14::InferenceEngine` 接入 ONNX Runtime CUDA ExecutionProvider，请求 CUDA 但环境不可用时优雅回退 CPU，并补可回退单测。

**Architecture:** 构造函数新增可选 `Ep` 参数（默认 `kCpu`，保 W15/旧码零改动）。CUDA 路径用 try 包住「append + Session 创建」整体，任意 `Ort::Exception` 后记录原因并用 CPU-only options 重建 session。库层不打日志，回退原因存成员供 demo/test 取用。实现分两阶段：先在现有 CPU 包上写完代码并验证回退（CPU 包请求 CUDA 必然抛错触发回退），再装 CUDA 12.3 工具链 + ORT GPU 包让 GPU 路径真正点亮。

**Tech Stack:** C++20, ONNX Runtime 1.26 C++ API, GoogleTest, CMake + Ninja, g++-15。

**关键前提（实现者必读）:** `AppendExecutionProvider_CUDA` 与 `OrtCUDAProviderOptions` 在所有 ORT 包的头文件里都有声明，因此**代码用当前 CPU 包就能编译**。CPU 包运行时请求 CUDA 会因找不到 `libonnxruntime_providers_cuda.so` 抛 `Ort::Exception` —— 这正是回退路径，可在 Phase 1 直接验证。

设计依据：`docs/superpowers/specs/2026-06-16-w14_5-cuda-ep-design.md`

---

## 文件结构

- 修改 `02_Inference_Analysis/w14_ort_basics/inference_engine.hpp` — 加 `Ep` 枚举、构造参数、两个访问器、两个成员。
- 修改 `02_Inference_Analysis/w14_ort_basics/inference_engine.cpp` — 构造函数 CUDA/CPU 双路径 + 整段 fallback。
- 修改 `02_Inference_Analysis/w14_ort_basics/inference_engine_test.cpp` — 加 3 个测试。
- 修改 `02_Inference_Analysis/w14_ort_basics/ort_basics_demo.cpp` — `--cuda` 开关 + 打印实际 EP / 回退原因。
- 修改 `README.md` — Phase 2：WSL CUDA 12.3 / cuDNN 9 / ORT GPU 包安装与排障命令。
- 修改 `02_Inference_Analysis/w14_ort_basics/notes.md`、`CLAUDE.md` — 进度同步。

CMakeLists 无需改动：默认 `ONNXRUNTIME_ROOT` 指向 CPU 包；Phase 2 用 `-DONNXRUNTIME_ROOT=<GPU 包路径>` 切换，现有 `BUILD_RPATH` 已覆盖 provider `.so`。

---

# Phase 1：代码（在当前 CPU 包上完成并验证）

## Task 1: 写失败测试（红）

**Files:**
- Test: `02_Inference_Analysis/w14_ort_basics/inference_engine_test.cpp`

- [ ] **Step 1: 在测试文件末尾（`InferenceEngineEnvTest` 之后）追加三个测试**

```cpp
// ---- W14.5：CUDA EP 接入 ----

// 默认构造行为不变：不传 ep 即 CPU EP、无回退原因。护住 W15 / 旧码零改动。
TEST_F(InferenceEngineModelTest, DefaultConstructionUsesCpu) {
  w14::InferenceEngine engine(kModelPath);
  EXPECT_EQ(engine.ActiveEp(), w14::Ep::kCpu);
  EXPECT_TRUE(engine.EpFallbackReason().empty());
}

// 请求 CUDA 永不抛异常：要么生效（ActiveEp==kCuda 且无原因），
// 要么回退 CPU（ActiveEp==kCpu 且有原因）。两环境下都成立。
// CPU 包运行时此处会触发真实回退，验证 fallback 路径。
TEST_F(InferenceEngineModelTest, CudaRequestFallsBackGracefully) {
  std::unique_ptr<w14::InferenceEngine> engine;
  ASSERT_NO_THROW(engine = std::make_unique<w14::InferenceEngine>(
                      kModelPath, w14::Ep::kCuda));
  if (engine->ActiveEp() == w14::Ep::kCpu) {
    EXPECT_FALSE(engine->EpFallbackReason().empty());
  } else {
    EXPECT_EQ(engine->ActiveEp(), w14::Ep::kCuda);
    EXPECT_TRUE(engine->EpFallbackReason().empty());
  }
}

// GPU 真跑：CUDA 可用时断言 EP 生效并跑通一次推理；不可用则优雅跳过。
TEST_F(InferenceEngineModelTest, LoadsModelWithCudaEp) {
  w14::InferenceEngine engine(kModelPath, w14::Ep::kCuda);
  if (engine.ActiveEp() != w14::Ep::kCuda) {
    GTEST_SKIP() << "CUDA EP 不可用，已回退 CPU：" << engine.EpFallbackReason();
  }
  std::vector<float> input(1 * 3 * 224 * 224, 0.5f);
  std::vector<int64_t> shape = {1, 3, 224, 224};
  auto outputs = engine.Run(input, shape);
  ASSERT_EQ(outputs.size(), 1u);
  auto out_info = outputs[0].GetTensorTypeAndShapeInfo();
  EXPECT_EQ(out_info.GetElementCount(), 1000u);
}
```

- [ ] **Step 2: 加 `<memory>` 头**

文件顶部 include 区（与现有 `<vector>` 等并列）补：

```cpp
#include <memory>
```

- [ ] **Step 3: 编译，确认因 API 不存在而失败（红）**

Run: `cmake --build build --target w14_inference_engine_test -j$(nproc)`
Expected: 编译失败，报 `w14::Ep`、`ActiveEp`、`EpFallbackReason` 未声明。

> build 目录若未配置：先 `cmake -B build -S . -DCMAKE_CXX_COMPILER=g++-15 -G Ninja`。

## Task 2: 实现 Ep API + 整段 fallback（绿）

**Files:**
- Modify: `02_Inference_Analysis/w14_ort_basics/inference_engine.hpp`
- Modify: `02_Inference_Analysis/w14_ort_basics/inference_engine.cpp`

- [ ] **Step 1: 头文件加 Ep 枚举（`namespace w14 {` 之后、类声明之前）**

```cpp
// ExecutionProvider 选择。kCuda 不可用时构造函数优雅回退 kCpu（见 .cpp）。
enum class Ep { kCpu, kCuda };
```

- [ ] **Step 2: 头文件改构造函数签名 + 加访问器**

将 `explicit InferenceEngine(const std::string& model_path);` 改为：

```cpp
  // ep 默认 kCpu，保持既有调用方（W15 Classifier / demo / 旧测试）零改动。
  // 请求 kCuda 但环境不可用时不抛异常，回退 CPU；用 ActiveEp() 查实际生效的 EP。
  explicit InferenceEngine(const std::string& model_path, Ep ep = Ep::kCpu);
```

在 `Outputs()` 访问器之后加：

```cpp
  // 实际生效的 EP。注意：仅表示 Session 成功请求/注册了该 EP，
  // 不证明每个算子都落到该设备——kernel placement 严格确认留 W16/W18 profiling。
  [[nodiscard]] Ep ActiveEp() const { return active_ep_; }
  // CUDA 回退到 CPU 的原因（Ort::Exception 文本）；未回退时为空串。
  [[nodiscard]] const std::string& EpFallbackReason() const {
    return ep_fallback_reason_;
  }
```

- [ ] **Step 3: 头文件加私有成员（`std::vector<IoInfo> outputs_;` 之后）**

```cpp
  Ep active_ep_ = Ep::kCpu;
  std::string ep_fallback_reason_;
```

- [ ] **Step 4: .cpp 重写构造函数**

将现有 `InferenceEngine::InferenceEngine(const std::string& model_path) { ... }` 整体替换为：

```cpp
InferenceEngine::InferenceEngine(const std::string& model_path, Ep ep) {
  // 提前给出友好错误，避免 ORT 抛 ORTCHAR 路径相关的晦涩异常。
  if (!std::filesystem::exists(model_path)) {
    throw std::runtime_error("[W14] 模型文件不存在: " + model_path);
  }

  // CUDA 路径：try 必须包住「append + Session 创建」整体——失败既可能发生在
  // AppendExecutionProvider_CUDA，也可能发生在 Session 创建阶段（provider .so
  // 找到了，但 libcudnn.so.9 / libcublas* dlopen 失败）。任意 Ort::Exception
  // 都记录原因后落到下面的 CPU 重建路径，不向上抛。
  if (ep == Ep::kCuda) {
    try {
      Ort::SessionOptions cuda_options;
      cuda_options.SetGraphOptimizationLevel(
          GraphOptimizationLevel::ORT_ENABLE_ALL);
      OrtCUDAProviderOptions cuda_opts{};  // device_id 默认 0
      cuda_options.AppendExecutionProvider_CUDA(cuda_opts);
      session_ = std::make_unique<Ort::Session>(GlobalEnv(), model_path.c_str(),
                                                cuda_options);
      active_ep_ = Ep::kCuda;
    } catch (const Ort::Exception& e) {
      ep_fallback_reason_ = e.what();  // 记录原因，回退 CPU
    }
  }

  // CPU 路径：默认请求 或 CUDA 失败回退。CPU 路径本身失败才真正抛错。
  if (!session_) {
    try {
      Ort::SessionOptions cpu_options;
      cpu_options.SetGraphOptimizationLevel(
          GraphOptimizationLevel::ORT_ENABLE_ALL);
      session_ = std::make_unique<Ort::Session>(GlobalEnv(), model_path.c_str(),
                                                cpu_options);
      active_ep_ = Ep::kCpu;
    } catch (const Ort::Exception& e) {
      throw std::runtime_error(std::string("[W14] ORT 加载失败: ") + e.what());
    }
  }

  // 构造期一次性查出 I/O 元数据缓存，Run 热路径不再触发 ORT 内部分配。
  try {
    Ort::AllocatorWithDefaultOptions allocator;

    const size_t input_count = session_->GetInputCount();
    inputs_.reserve(input_count);
    for (size_t i = 0; i < input_count; ++i) {
      inputs_.push_back(
          MakeIoInfo(session_->GetInputNameAllocated(i, allocator),
                     session_->GetInputTypeInfo(i)));
    }

    const size_t output_count = session_->GetOutputCount();
    outputs_.reserve(output_count);
    for (size_t i = 0; i < output_count; ++i) {
      outputs_.push_back(
          MakeIoInfo(session_->GetOutputNameAllocated(i, allocator),
                     session_->GetOutputTypeInfo(i)));
    }
  } catch (const Ort::Exception& e) {
    throw std::runtime_error(std::string("[W14] ORT 元数据查询失败: ") +
                             e.what());
  }
}
```

- [ ] **Step 5: 编译并跑测试（绿）**

Run: `cmake --build build --target w14_inference_engine_test -j$(nproc) && ctest --test-dir build -R "W14_" --output-on-failure`
Expected: 全绿。当前 CPU 包下：`DefaultConstructionUsesCpu` 通过；`CudaRequestFallsBackGracefully` 走回退分支通过（`ActiveEp()==kCpu` 且 reason 非空）；`LoadsModelWithCudaEp` 因回退而 `GTEST_SKIP`。原有 4 个测试保持通过。

- [ ] **Step 6: 格式检查**

Run: `find . -maxdepth 3 -regex '.*0[1-4]_.*' \( -name "*.cpp" -o -name "*.hpp" \) | xargs clang-format-21 --dry-run --Werror`
Expected: 无输出（通过）。有差异则 `clang-format-21 -i <文件>` 修复后重跑。

- [ ] **Step 7: Commit**

```bash
git add 02_Inference_Analysis/w14_ort_basics/inference_engine.hpp \
        02_Inference_Analysis/w14_ort_basics/inference_engine.cpp \
        02_Inference_Analysis/w14_ort_basics/inference_engine_test.cpp
git commit -m "feat(W14.5): InferenceEngine 支持可选 CUDA EP + 优雅回退

- 新增 Ep 枚举与构造参数（默认 kCpu，旧调用方零改动）
- CUDA 失败时整段 Session fallback 到 CPU，原因存 EpFallbackReason()
- 补 3 个测试：默认 CPU / 优雅回退 / CUDA 真跑(可跳过)

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>" \
  --author="hanchunbo <hanchunbo@users.noreply.github.com>"
```

## Task 3: demo 加 `--cuda` 开关

**Files:**
- Modify: `02_Inference_Analysis/w14_ort_basics/ort_basics_demo.cpp`

- [ ] **Step 1: main 起始处加 EP 变量与参数解析**

将 `std::string model_path = "...";` 之后、`for` 循环之前加：

```cpp
  w14::Ep ep = w14::Ep::kCpu;
```

在 `for` 循环里，`--model` 分支之后、`-h/--help` 分支之前插入：

```cpp
    } else if (arg == "--cuda") {
      ep = w14::Ep::kCuda;
```

并把 help 文本改为：

```cpp
      std::cout << "用法: w14_ort_basics_demo [--model <path>] [--cuda]\n";
```

- [ ] **Step 2: 用 ep 构造引擎并打印实际 EP / 回退原因**

将 `w14::InferenceEngine engine(model_path);` 改为：

```cpp
    w14::InferenceEngine engine(model_path, ep);

    const char* ep_name =
        engine.ActiveEp() == w14::Ep::kCuda ? "CUDA" : "CPU";
    std::cout << std::format("[W14] 实际 EP: {}\n", ep_name);
    if (!engine.EpFallbackReason().empty()) {
      std::cout << std::format("[W14] CUDA 回退原因: {}\n",
                               engine.EpFallbackReason());
    }
```

- [ ] **Step 3: 编译并手动验证回退打印**

Run: `cmake --build build --target w14_ort_basics_demo -j$(nproc) && ./build/02_Inference_Analysis/w14_ort_basics/w14_ort_basics_demo --cuda`
Expected: 当前 CPU 包下打印 `实际 EP: CPU` 且有一行 `CUDA 回退原因: ...`；推理正常完成。

- [ ] **Step 4: 格式检查 + Commit**

```bash
find . -maxdepth 3 -regex '.*0[1-4]_.*' \( -name "*.cpp" -o -name "*.hpp" \) | xargs clang-format-21 --dry-run --Werror
git add 02_Inference_Analysis/w14_ort_basics/ort_basics_demo.cpp
git commit -m "feat(W14.5): demo 加 --cuda 开关，打印实际 EP 与回退原因

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>" \
  --author="hanchunbo <hanchunbo@users.noreply.github.com>"
```

---

# Phase 2：环境（仅在本地 GPU 机执行，点亮 CUDA 路径）

> 这一阶段是环境供给 + 文档，无 TDD 代码。验证手段是重跑 ctest，观察
> `LoadsModelWithCudaEp` 从 skip 变为真跑、`ActiveEp()==kCuda`。
> **WSL 强约束：绝不装 `cuda` / `cuda-drivers`（driver 归 Windows host）。**

## Task 4: 装 CUDA 12.3 工具链 + cuDNN 9 + ORT GPU 包

**Files:**
- Modify: `README.md`（前提条件 / W14+ 额外依赖节）

- [ ] **Step 1: 核对版本要求**

打开 ORT CUDA EP requirements 页确认 1.26 对 CUDA / cuDNN 的精确要求，确认本计划 pin 的
CUDA 12.3 + cuDNN 9 落在兼容范围内（cuDNN 8/9 不可混）。
- https://onnxruntime.ai/docs/execution-providers/CUDA-ExecutionProvider.html

- [ ] **Step 2: 装 CUDA 12.3 工具链（不带 driver）**

```bash
# 加 NVIDIA WSL-Ubuntu apt 源（若未加过）
wget https://developer.download.nvidia.com/compute/cuda/repos/wsl-ubuntu/x86_64/cuda-keyring_1.1-1_all.deb
sudo dpkg -i cuda-keyring_1.1-1_all.deb
sudo apt-get update
# 只装具体工具链包，绝不装 cuda / cuda-drivers
sudo apt-get install -y cuda-toolkit-12-3
```

- [ ] **Step 3: 装 cuDNN 9**

```bash
sudo apt-get install -y cudnn9-cuda-12
```

- [ ] **Step 4: 验证工具链可见**

```bash
nvidia-smi
/usr/local/cuda-12.3/bin/nvcc --version   # 或将 /usr/local/cuda/bin 加入 PATH 后 nvcc --version
ldconfig -p | grep -E 'cudnn|cudart|cublas'
```
Expected: nvidia-smi 显示 RTX 3060；nvcc 报 12.3；ldconfig 列出 cudnn9 / cudart / cublas。
若 ldconfig 查不到 CUDA 库：把 `/usr/local/cuda-12.3/lib64` 写入 `/etc/ld.so.conf.d/` 后 `sudo ldconfig`。

- [ ] **Step 5: 下 ORT GPU 包并与 CPU 包并排放置**

```bash
cd third_party/onnxruntime
wget https://github.com/microsoft/onnxruntime/releases/download/v1.26.0/onnxruntime-linux-x64-gpu-1.26.0.tgz
tar xzf onnxruntime-linux-x64-gpu-1.26.0.tgz
ls onnxruntime-linux-x64-gpu-1.26.0/lib/libonnxruntime_providers_cuda.so  # 确认存在
cd -
```

- [ ] **Step 6: 用 GPU 包重新配置并跑测试**

```bash
cmake -B build-gpu -S . -DCMAKE_CXX_COMPILER=g++-15 -G Ninja \
  -DONNXRUNTIME_ROOT="$(pwd)/third_party/onnxruntime/onnxruntime-linux-x64-gpu-1.26.0"
cmake --build build-gpu --target w14_inference_engine_test -j$(nproc)
ctest --test-dir build-gpu -R "W14_" --output-on-failure
```
Expected: `LoadsModelWithCudaEp` 不再 skip，通过（内部 `ActiveEp()==kCuda`）。其余测试通过。

> 验证 `ActiveEp()==kCuda` 真生效（可选）：
> `./build-gpu/02_Inference_Analysis/w14_ort_basics/w14_ort_basics_demo --cuda`
> 应打印 `实际 EP: CUDA` 且无回退原因。

- [ ] **Step 7: README 补环境文档**

在 `README.md` 的「前提条件」/「W14+ 额外依赖」节补：
- WSL 装 CUDA 12.3 工具链命令（强调不装 driver / cuda-drivers）
- cuDNN 9 安装命令
- ORT GPU 包下载命令
- 排障三件套：`nvidia-smi`、`nvcc --version`、`ldconfig -p | grep cudnn`
- 用 `-DONNXRUNTIME_ROOT=<GPU 包路径>` 切换到 GPU 包构建

- [ ] **Step 8: Commit**

```bash
git add README.md
git commit -m "docs(W14.5): README 补 WSL CUDA 12.3 / cuDNN 9 / ORT GPU 包安装与排障

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>" \
  --author="hanchunbo <hanchunbo@users.noreply.github.com>"
```

## Task 5: 进度同步收尾

**Files:**
- Modify: `02_Inference_Analysis/w14_ort_basics/notes.md`
- Modify: `CLAUDE.md`

- [ ] **Step 1: notes.md「待补」节勾销第 1、3 项**

把 CUDA EP、`LoadsModelWithCudaEp` 两项标记为 FIXED（保留 ResNet18 第 2 项、VPS 第 4 项），
并简述实现：可选 Ep 参数 + 整段 Session fallback + `EpFallbackReason()`。

- [ ] **Step 2: CLAUDE.md「当前进度」更新**

把「CUDA EP（W14.5）仍待补」改为 W14.5 已完成的描述（CUDA EP 接入 + 可回退测试），
并注明 ResNet18 对比与 VPS CPU EP 仍待。

- [ ] **Step 3: 验收复核**

确认三条验收标准（见 spec §7）：
1. CPU 包/CPU 环境 W14/W15 全绿、CUDA 测试 skip；
2. GPU 包+CUDA 12.3+cuDNN9 环境 `LoadsModelWithCudaEp` 不 skip 且 `ActiveEp()==kCuda`；
3. README 含安装与三条排障命令。

- [ ] **Step 4: Commit**

```bash
git add 02_Inference_Analysis/w14_ort_basics/notes.md CLAUDE.md
git commit -m "docs(W14.5): 进度同步 + 遗留清单勾销 CUDA EP/单测项

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>" \
  --author="hanchunbo <hanchunbo@users.noreply.github.com>"
```

---

## 完成后

合入 `dev`（Claude 可自动做）。合入 `main` 必须先问用户（见 CLAUDE.md「Branch & Merge Policy」）。
